{
  "name": "seatlight-backend",
  "version": "1.0.0",
  "description": "SeatLight backend with Feishu OAuth and auto-refresh",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
