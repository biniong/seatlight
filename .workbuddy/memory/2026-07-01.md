# 2026-07-01

## 下拉框不显示问题修复
- 根因：Venues 表 ID 中大写 I 被误写为小写 l（`tblKw40knld48WpO` → `tblKw40knId48WpO`）
- 所有 app token 读取 Venues 表的操作都返回 TableIdNotFound
- 修复：1. 修正 server.js 中两处表 ID 拼写  2. venues/list 从 user token 改为 app token
- 已部署到 Railway

## Banner 加载改为飞书表格优先 + 本地 fallback
- 新增 `loadBannerFromApi()` 函数，邀请码验证后调用 `/api/banner/list`
- 加载成功且数据有效 → 替换 bannerSlides 并重新 renderBanner()
- 网络失败/数据为空 → 保留本地 base64 写死的 3 张图片
- 修复 renderBanner 定时器累积问题（新增 bannerInterval 变量，每次渲染前清除）
- 已部署到 Railway

## Banner 新增 FIVE O ONE 记录
- 通过 SeatLight 服务器 `/api/upload` 上传图片（用 user_access_token）
- 通过 `/api/banner/create` 创建记录（record_id: recvo7I2hcWIx2）
- 注意：banner/create 需要 `file_token` 字段（snake_case），不是 `fileToken`
- app_access_token 无法用于图片上传（drive upload_all 需要 user token）

## Banner 自动加载优化
- 问题：用户刷新页面看不到新图，需要开无痕 + 输入邀请码
- 根因：浏览器缓存旧版 HTML/JS
- 修复：
  1. 静态文件添加 `Cache-Control: no-cache` 响应头
  2. Banner API 超时从 3s 增至 5s（飞书 API 响应约 2.4s）
  3. 页面加载时自动调用 `loadBannerFromApi()`（无需邀请码）
  4. `/api/banner/list` 设为公开接口（无需邀请码）
- 部署：commit bcdb1e8
